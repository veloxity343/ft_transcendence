# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to my `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/transcendence?schema=public"

# Server
ENVIRONMENT=DEVELOPMENT
BACK_PORT=3000
FRONT_PORT=5173
FRONT_URL=http://localhost:5173
SITE_URL=http://localhost

# JWT
JWT_SECRET=my-super-secret-jwt-key-change-this
ACCESS_TOKEN_EXPIRATION=15m
REFRESH_TOKEN_EXPIRATION=7d

# 42 OAuth
FORTYTWO_ID=my-42-app-id
FORTYTWO_SECRET=my-42-app-secret
FORTYTWO_CALLBACK=http://localhost:3000/auth/42/callback

# 2FA
MY_2FA_APP_NAME=ft_transcendence

# Upload
UPLOAD_DIR=./uploads
DEFAULT_AVATAR=https://cdn.intra.42.fr/users/default.png
